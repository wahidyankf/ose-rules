node_modules/
worktrees/

# The shared plan-structure fixture corpus is byte-identical across implementations.
# Formatting it would change bytes that other repositories verify by digest.
specs/fixtures/

# Rhino owns these adapter bytes. Formatting generated outputs would make them
# diverge from the canonical `.agents/` source; regenerate them instead.
.claude/agents/
.claude/skills/
.codex/agents/
.opencode/agents/
