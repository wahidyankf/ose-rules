#!/usr/bin/env bash
# The last local moment before anything reaches a remote. Git's ref-update
# lines arrive on stdin and are forwarded unchanged, so a gate that screens the
# outbound ref reads what Git is actually about to send.
set -euo pipefail

exec ./rhino gate run --surface pre-push -- "$@"
