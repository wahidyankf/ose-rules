{
  "name": "ose-rules",
  "version": "0.0.0",
  "description": "A reference catalog of governance, planning, agent, and skill artifacts that other repositories may adopt",
  "private": true,
  "license": "MIT",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "prepare": "husky",
    "format": "prettier --write .",
    "check:complete": "./rhino gate run --surface ci"
  },
  "devDependencies": {
    "husky": "^9.1.7",
    "markdownlint-cli2": "^0.23.2",
    "prettier": "^3.4.2"
  },
  "overrides": {
    "smol-toml": "^1.8.0"
  }
}
