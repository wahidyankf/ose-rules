{
  "name": "ose-rules",
  "version": "0.0.0",
  "description": "A reference catalog of governance, planning, agent, and skill artifacts that other repositories may adopt",
  "private": true,
  "license": "MIT",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "prepare": "husky",
    "format": "./hippo run --class transactional --resource-tier standard --disk-path . -- prettier --write .",
    "check:hippo-consumer": "./hippo run --class ephemeral --resource-tier light --disk-path . -- ./scripts/check-hippo-consumer.sh",
    "check:complete": "npm run check:hippo-consumer && ./hippo run --class ephemeral --resource-tier heavy --disk-path . -- ./rhino gate run --surface ci"
  },
  "devDependencies": {
    "husky": "^9.1.7",
    "markdownlint-cli2": "^0.23.2",
    "prettier": "^3.4.2"
  },
  "overrides": {
    "smol-toml": "^1.8.0"
  }
}
